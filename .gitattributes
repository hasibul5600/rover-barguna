# Normalise line endings. Development happens on Windows but Vercel builds on
# Linux, so everything is stored as LF in the repository regardless of checkout.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
